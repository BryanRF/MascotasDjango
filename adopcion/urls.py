# adopcion/urls.py

from django.urls import path
from . import views

urlpatterns = [
    # path('adopcion/create/', views.adopcion_create, name='adopcion_create'),
    # path('adopcion/list/', views.adopcion_list, name='adopcion_list'),
    # path('adopcion/detail/<str:id_adopcion>/', views.adopcion_detail, name='adopcion_detail'),
    # path('adopcion/update/<str:id_adopcion>/', views.adopcion_update, name='adopcion_update'),
    # path('adopcion/delete/<str:id_adopcion>/', views.adopcion_delete, name='adopcion_delete'),
]
